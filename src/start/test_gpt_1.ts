// 🟢 Уровень 1 — Лёгкий
// 1. Что выведет TypeScript?
// let age: number = 25;
// age = "30";
// Будет ошибка потому что мы на строке let age: number = 25 тип age является number а мы присваиваем string "30"

// 2. Допиши типы:
// function greet(name: ???): ??? {
//   return `Hello, ${name}`;
// }
// function greet(name: string): string {
// 	return `Hello, ${name}`;
// }

// 3. Что не так?
// let isActive: boolean = "true";
// тип переменной isActive является boolean а мы присваиваем string "true"

// 4. Напиши тип для переменной:
// let users = ["Alice", "Bob", "Charlie"];
// // users: ???
// let users:Array<string> = ["Alice", "Bob", "Charlie"];
// users: Array<string>

// 5. Что выведет консоль?
// let a: any = 5;
// a = "hello";
// console.log(a);
// Unexpected token ':' у меня так но я не понял почему именно
// Логично что any это без проверки типа но при первом случаем = 5 оно становится типа number
// Но там any по идеии будет hello но и может быть runtime error покрайней мере я так знаю

// 6. Напиши тип функции, которая принимает два числа и возвращает их сумму.
// function sum(a: number, b: number): number {
// 	return a + b;
// }

// 7. Что делает as в TypeScript?
// Это форма приведения типа напрмиер:
// let x:any = 7;
// let y = x as string; // Типа расматриваю переменную x как стринг(если не прав уточни)


// 8. Какие бывают типы переменных в TS кроме number, string, boolean?
// number, string, boolen, array, any, unknown, never, void
// Partial,Required,Pick,Omit

// 9. В чём разница между any и unknown?
// unknown это безопасная форма any но используется только после проверки типа

// 10. Что делает readonly в интерфейсах?
// readonly это типа доступно для чтения т.е после реализации интерфейса менять данные не получится у поле
// которого есть readonly

// ================================================================================

// ⚡️ Микротест: as и unknown (5 заданий)
// 1. Что выведет консоль?
// let value: unknown = "hello";
// console.log((value as string).toUpperCase());
// a) HELLO
// b) Ошибка на компиляции
// c) undefined
// d) Runtime error
// Будет a потому что мы подсказывает компилятору что тип string

// 2. Что будет в результате выполнения?
// let value: unknown = 42;
// let length = (value as string).length;
// console.log(length);
// a) Ошибка в рантайме
// b) 2
// c) Ошибка компиляции
// d) undefined
// length - вызовет ошибку синтаксиса,но если не обращать на имя переменной то 
// ответ будет undefined потому что даже если мы подсказываем комплитятору что тип
// string но свойство length у number нет и соответственно undefined

// 3. Чем отличается as от <> в TypeScript?
// a) as — это runtime cast, <> — compile time
// b) as и <> — одно и то же, но <> не работает в JSX
// c) as строгий, <> нет
// d) <> работает только с any, а as — с любыми типами
// Оба нужны для утверждения типа но не будет работать в jsx <> потому что это короткая форма фрагмента
// поэтому ответ b

// 4. Почему такой код может быть опасным?
// let userInput: any = "123";
// let num: number = userInput as number;
// a) Потому что это даст ошибку при компиляции
// b) Потому что as не проверяет тип реально
// c) Потому что as не работает с any
// d) Потому что userInput нельзя привести к числу
// any отключает проверку типа а мы еще убеждаем что это число хотя является строкой
// тут я точно не могу ответить потому что и ошибки могут быть вариант a и b
// но больше тянусь к a потому что реально можно вызвать ошибку

// 5. Как безопасно извлечь значение из unknown?
// a) Использовать as без проверок
// b) Проверить тип с помощью typeof или instanceof, потом применить as
// c) Просто присвоить переменной типа any
// d) Нельзя работать с unknown
// тут b